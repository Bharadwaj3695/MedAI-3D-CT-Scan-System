"""
Supabase Client Module (Deprecated).

This module is deprecated in favor of the singleton client and dependency injection
helper in `backend.database`. It is preserved for backwards compatibility with
legacy scripts.
"""

from backend.database import supabase